import React from 'react';
import { Card } from 'antd';
import LoadPatient from '../Load/LoadPatient';

const Student = () => (
  <Card style={{ width: 300 }}>
    <LoadPatient />
  </Card>
);

export default Student;

// complete case, reveal examination
